import DefaultLayout from '@layouts/DefaultLayout';

const CardsPage = () => {
  return <>CardsPage</>;
};

CardsPage.getLayout = (page: any) => <DefaultLayout title="Cards">{page}</DefaultLayout>;

export default CardsPage;
